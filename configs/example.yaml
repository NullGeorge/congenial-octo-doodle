agent:
  id: server-01

control:
  endpoint: ""
  token: ""
  timeout: 10s

knockd:
  config: /etc/knockd.conf
  service: knockd

storage:
  path: /var/lib/knockd-agent/state.db

logging:
  level: info
